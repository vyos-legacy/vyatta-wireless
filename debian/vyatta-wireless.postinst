#!/bin/bash

if [ -n "$DEBIAN_SCRIPT_DEBUG" ]; then
    set -v -x DEBIAN_SCRIPT_TRACE=1;
fi
${DEBIAN_SCRIPT_TRACE:+ echo "#42#DEBUG# RUNNING $0 $*"}
set -e
set -u

# Make directories for config files
mkdir -m 755 -p /var/run/hostapd /var/run/wpa_supplicant
